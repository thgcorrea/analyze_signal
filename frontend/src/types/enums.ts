export enum TrendType {
  ASCENDING = 'ascending',
  DESCENDING = 'descending',
  STABLE = 'stable',
}

export enum ResultCardColor {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  SUCCESS = 'success',
  WARNING = 'warning',
  ERROR = 'error',
}
